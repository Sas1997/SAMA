# setup.py — compatibility shim for older tools (pip < 21.3, conda, etc.)
# All metadata and configuration lives in pyproject.toml.
# Do not add arguments to setup() here.
from setuptools import setup

setup()
